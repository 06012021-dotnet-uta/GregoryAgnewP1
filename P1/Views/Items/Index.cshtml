@model IEnumerable<DatabaseModels.Item>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Item1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descriptionforconsole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Item1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descriptionforconsole)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Itemid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Itemid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Itemid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
